<ul>
<% @college_results.each do |college| %>

<li> <%= link_to college.name, college_path(college) %> </li>

<% end %> 
</ul>

<%= form_tag("/name_search", method: "get") do %>
    # <%= label_tag(:name, "Search by Name") %>
    # <%= text_field_tag(:name) %>
    # <%= submit_tag("Search") %> 
   <% end %> 